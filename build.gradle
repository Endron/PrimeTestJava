plugins { id 'java' }

sourceCompatibility = "1.8"
targetCompatibility = "1.8"

tasks.withType(JavaCompile) { options.encoding = 'UTF-8' }

repositories { maven { url "http://nexus.bybn.de/content/groups/public" } }

dependencies { testCompile 'junit:junit:4.11' }
